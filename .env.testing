APP_ENV=testing
APP_KEY=base64:DNvPcfyKhOhCd10DBP/0kAGdDthUVNBWP8oQP+UULbs=

# Enable live IDX smoke tests during the test run
RUN_LIVE_IDX_TESTS=1

# Ensure queued jobs run immediately in tests
QUEUE_CONNECTION=sync

# Use SQLite for tests to avoid external DB dependency
DB_CONNECTION=sqlite
DB_DATABASE=:memory:
